/**
 * 공유 정렬/필터링 타입
 * community, my-roadmaps 등 여러 feature에서 사용
 */

export type SortOrder = 'asc' | 'desc';

export type SortBy = 'name' | 'recent' | 'size';

export type FilterCategory = 'all' | 'roadmap' | 'directory';
