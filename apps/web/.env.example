# ─── Public (client-side) ───────────────────────────────────────────────
# 배포 타깃: Vercel (공식) — Dockerfile/netlify.toml 은 참고용으로만 유지.
# 프록시 모드: NEXT_PUBLIC_API_URL=/api → Next.js proxy 경유 (CSRF 활성)
# 직접 모드: NEXT_PUBLIC_API_URL=https://api.example.com → 백엔드 직접 호출
NEXT_PUBLIC_API_URL=/api

# Nest Socket.IO origin. 경로를 붙이지 않고 API origin만 지정한다.
NEXT_PUBLIC_REALTIME_URL=https://api.example.com

# 사이트 절대 URL. robots/sitemap/JSON-LD/OpenGraph 에서 사용.
NEXT_PUBLIC_SITE_URL=https://example.com

# 환경 이름 ("production" | "staging" | "preview" | "development").
# production 이외 환경에서는 robots 가 noindex 응답한다.
NEXT_PUBLIC_ENV=production

# Feature flags
NEXT_PUBLIC_API_MOCKING=false
NEXT_PUBLIC_REALTIME_ENABLED=false
NEXT_PUBLIC_AI_FEATURES_ENABLED=false
# UI visibility only; the Nest API kill switches remain the authorization boundary.
NEXT_PUBLIC_EVIDENCE_EXECUTION_ENABLED=false
NEXT_PUBLIC_PROOF_PROFILE_ENABLED=false

# ─── Server-only (client 번들에 노출되지 않음) ──────────────────────────
# API 프록시(`src/app/api/[...path]/route.ts`) 의 upstream origin.
# 경로, 쿼리, 해시 없이 origin만 지정한다. 프로덕션에서는 HTTPS가 필수다.
API_ORIGIN=https://api.example.com

# Sentry (#204) — 프로덕션 배포에서 주입
# SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=

# ─── CSRF ───────────────────────────────────────────────────────────────────
# 토큰은 런타임 생성 — 별도 env 불필요.
# GET /api/csrf-token 발급 → X-CSRF-Token 헤더로 뮤테이션 요청에 자동 포함.
# 프록시 모드(NEXT_PUBLIC_API_URL=/api)에서만 활성화됨.
