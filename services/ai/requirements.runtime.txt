# Runtime-only dependencies for the production image.
#
# Keep test, lint, and type-check tooling in requirements.txt for CI and the
# development image. The active application uses FAISS with deterministic
# in-process embeddings; sentence-transformers is not imported by production
# code and is intentionally not installed here (it would pull a CUDA-enabled
# Torch stack into the CPU-only server image).

# Django + REST API
Django>=4.2,<5.0
djangorestframework>=3.15,<4.0
drf-spectacular>=0.27.2

# Production ASGI server and static assets
gunicorn>=22.0.0
uvicorn[standard]>=0.30.0
whitenoise>=6.7.0

# AI/LLM clients
google-genai>=1.0.0
tavily-python>=0.5.0
exa-py>=1.8.0

# RAG and vector search used by the application
langchain>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-text-splitters>=0.3.0
faiss-cpu>=1.8.0
numpy>=1.26,<2.0
scikit-learn>=1.5.0
networkx>=3.3
rank-bm25>=0.2.2

# Configuration, logging, HTTP, and persistence
pydantic>=2.8.0
pydantic-settings>=2.4.0
packaging>=23.2,<26
structlog>=24.4.0
python-json-logger>=2.0.7
httpx>=0.27.0
tenacity>=9.0.0
aiohttp>=3.10.0
python-dotenv>=1.0.1
orjson>=3.10.0
cachetools>=5.5.0
python-dateutil>=2.9.0

# Security, CORS, and PostgreSQL
django-cors-headers>=4.4.0
PyJWT>=2.9.0,<3.0
psycopg[binary]>=3.2.0,<4.0
